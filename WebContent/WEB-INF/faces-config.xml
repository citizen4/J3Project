<?xml version='1.0' encoding='UTF-8'?>
<faces-config version="2.2" xmlns="http://xmlns.jcp.org/xml/ns/javaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
    http://xmlns.jcp.org/xml/ns/javaee/web-facesconfig_2_2.xsd">


    <!--
    <navigation-rule>
        <display-name>index.xhtml</display-name>
        <from-view-id>/index.xhtml</from-view-id>

        <navigation-case>
            <from-action>#{customerBean.customerValid}</from-action>
            <from-outcome>correct</from-outcome>
            <if>#{customerBean.under18}</if>
            <to-view-id>/childvideo.xhtml</to-view-id>
            <redirect />
        </navigation-case>
        <navigation-case>
            <from-action>#{customerBean.doLogin}</from-action>
            <from-outcome>correct</from-outcome>
            <if>#{!customerBean.under18}</if>
            <to-view-id>/video.xhtml</to-view-id>
            <redirect />
        </navigation-case>
        <navigation-case>
            <from-action>#{customerBean.doValidateCustomer}</from-action>
            <from-outcome>invalid</from-outcome>
            <to-view-id>/index.xhtml</to-view-id>
        </navigation-case>
        <navigation-case>
            <from-action>#{customerBean.doValidateCustomer}</from-action>
            <from-outcome>valid</from-outcome>
            <to-view-id>/showCustomer.xhtml</to-view-id>
        </navigation-case>

    </navigation-rule>-->

</faces-config>
